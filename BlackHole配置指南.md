# BlackHole 音频路由配置指南

BlackHole 已安装。需要手动在系统设置里完成一次性配置。

---

## 目标

让你开会时，**麦克风（你的声音）+ 会议软件（对方声音）** 同时被录进同一个文件。

---

## 一次性配置步骤（5分钟）

### 第一步：打开"音频 MIDI 设置"

- 按 `Cmd + 空格`，搜索「音频 MIDI 设置」打开
- 或：应用程序 → 实用工具 → 音频 MIDI 设置

---

### 第二步：创建「多输出设备」（让你同时能听见且能被录制）

1. 点击左下角 **+** → 选「创建多输出设备」
2. 右侧勾选：
   - ✅ **BlackHole 2ch**
   - ✅ **MacBook Pro 扬声器**（或你的耳机）
3. 双击名称，改为：`会议录制输出`
4. 确保「主设备」选的是你的扬声器/耳机（否则没声音）

---

### 第三步：创建「聚合设备」（录音时同时收两路声音）

1. 点击左下角 **+** → 选「创建聚合设备」
2. 右侧勾选：
   - ✅ **BlackHole 2ch**
   - ✅ **MacBook Pro 麦克风**（或外接麦克风）
3. 双击名称，改为：`会议录制输入`
4. 时钟源选「BlackHole 2ch」

---

### 第四步：每次开会前设置

| 场景 | 会议软件音频输出设置 | 录音软件音频输入设置 |
|------|---------------------|---------------------|
| Zoom / 腾讯会议 / Teams / 任意软件 | `会议录制输出` | `会议录制输入` |

**具体操作：**

**Zoom**：设置 → 音频 → 扬声器 → 选「会议录制输出」

**腾讯会议**：设置 → 音频 → 扬声器 → 选「会议录制输出」

**系统级输出**（影响所有软件）：
- 系统设置 → 声音 → 输出 → 选「会议录制输出」

---

## 录音方法（两种选一）

### 方法 A：QuickTime 录音（最简单）

```
1. 打开 QuickTime Player
2. 文件 → 新建音频录制
3. 点录音按钮旁的小箭头 ▼ → 选「会议录制输入」
4. 点红色录音键 → 开始录制
5. 开会结束后停止，保存为 .m4a 文件
```

### 方法 B：命令行录制（自动化）

```bash
# 自动录音，按 Ctrl+C 停止，文件自动保存到 ~/meeting-ai/recordings/
~/meeting-ai/scripts/record.sh
```

---

## 验证配置是否正确

在 QuickTime 里录 30 秒，播放后：
- ✅ 能听到你说话 → 麦克风配置正确
- ✅ 能听到对方声音 → 会议软件路由正确
- ❌ 只有你的声音 → 检查会议软件输出是否选「会议录制输出」
- ❌ 完全没声音 → 检查聚合设备是否勾选了 BlackHole 2ch

---

## 注意事项

- 配置完成后**永久有效**，以后开会前只需确认会议软件输出设置即可
- 如果升级 macOS 后 BlackHole 消失，重新运行：`brew reinstall blackhole-2ch`
- 耳机用户：在「多输出设备」里把耳机加进去代替扬声器

# 会议 AI 工具链 — 安装汇总报告

**整体耗时：** 约 75 分钟（含等待网络下载时间）
**整体占用磁盘：** 约 19GB（其中 17GB 是 Ollama 大模型）

---

## 一、按模块分类的安装结果

### ① 录音环节（音频路由）

| 项目 | 状态 | 说明 |
|------|------|------|
| BlackHole 2ch | ✅ 已就绪 | 此前已安装，免费虚拟音频驱动 |
| ffmpeg | ✅ 已就绪 | 此前已安装，音视频处理 |
| Audio MIDI 配置 | ⚠️ 待你手动配置 | 见 `BlackHole配置指南.md`（5分钟操作） |

### ② 转录环节（语音 → 文字）

**位置：** `~/meeting-ai/whisper-env/`（1.0GB）

| 包 | 版本 | 用途 |
|------|------|------|
| Python | 3.11.14 | 运行环境（uv 自动管理） |
| PyTorch | 2.11.0 | 深度学习框架 |
| faster-whisper | 1.2.1 | Whisper 转录引擎（CPU 优化） |
| WhisperX | latest | 转录 + 时间戳对齐 |
| pyannote.audio | latest | 说话人区分 |
| transformers | 4.45+ | 神经网络模型支持 |

### ③ 摘要环节（文字 → 整理）

**位置：** `~/.ollama/models/`（已占 17GB）

| 模型 | 大小 | 状态 | 用途 |
|------|------|------|------|
| qwen2.5:14b | 9.0GB | 🔄 下载中 79%（约 19 分钟） | 中文摘要 / 去口水词（主力） |
| qwen2.5-coder:14b | 9.0GB | ✅ 已有 | 代码相关任务（你原有） |
| ~~qwen14b-32k~~ | ~~9GB~~ | 🗑️ 已删除 | 旧版，被 qwen2.5:14b 替代 |

### ④ 朗读环节（文字 → 语音）

**位置：** `~/meeting-ai/tts-env/`（594MB）+ macOS 系统内置（0MB）

| 项目 | 状态 | 用途 |
|------|------|------|
| macOS 内置 say 命令 | ✅ 系统自带 | Tingting/Meijia 等中文声音（默认引擎） |
| kokoro-onnx | ✅ 已装 | 高质量神经网络 TTS（可选增强） |
| ~~F5-TTS~~ | ❌ 放弃 | gradio 依赖反复下载失败 |

---

## 二、按时间顺序的尝试记录

| 阶段 | 操作 | 结果 |
|------|------|------|
| 1 | 检查环境，发现已有 brew/uv/ollama/ffmpeg/BlackHole | ✅ 节省大量时间 |
| 2 | 创建目录结构 + 两个 Python 3.11 venv | ✅ 5秒完成 |
| 3 | 并行装 PyTorch（两个环境） | ✅ 各约 3 分钟 |
| 4 | 启动拉取 qwen2.5:32b（20GB）| ❌ 你叫停了，节省了 16GB 内存占用 |
| 5 | 删除旧 qwen14b-32k，改下 qwen2.5:14b | 🔄 下载中 |
| 6 | 装 WhisperX（PyPI 直连） | ❌ pandas 下载超时 |
| 7 | 装 F5-TTS（PyPI 直连） | ❌ safehttpx 下载超时 |
| 8 | 改用清华镜像装 WhisperX | ❌ 清华没有此包 |
| 9 | 改用阿里云镜像装 faster-whisper | ✅ 3 分钟成功 |
| 10 | 阿里云 + PyPI 混合装 WhisperX | ✅ 终于成功 |
| 11 | 阿里云装 pyannote.audio | ✅ 成功，但版本冲突 |
| 12 | 升级 transformers 修冲突 | ✅ 修复完成 |
| 13 | 反复尝试 F5-TTS | ❌ 多镜像都因 gradio 依赖失败 |
| 14 | 改装 kokoro-onnx 替代 | ✅ 成功 |
| 15 | 编写 6 个自动化脚本 + 配置指南 | ✅ 完成 |

---

## 三、最终文件结构

```
~/meeting-ai/                          (1.6GB)
├── BlackHole配置指南.md              ← 一次性配置参考
├── 安装汇总报告.md                    ← 本文件
├── ollama_pull.log                    ← 模型下载日志（可删）
├── whisper-env/        (1.0GB)        ← 转录 Python 环境
├── tts-env/            (594MB)        ← 朗读 Python 环境
├── recordings/         (空)           ← 你的会议录音放这里
├── output/             (空)           ← 自动生成的逐字稿/摘要
└── scripts/            (48KB)
    ├── meeting.sh             ← 全流程一键脚本
    ├── transcribe.sh          ← 仅转录
    ├── summarize.sh           ← 仅摘要
    ├── read.sh                ← 朗读逐字稿
    ├── record.sh              ← 命令行录音
    ├── setup-hf-token.sh      ← 配置说话人区分令牌
    └── *.py                   ← 对应的 Python 实现
```

---

## 四、内存占用预估（运行时）

| 同时运行的程序 | 实际占用 |
|----------------|----------|
| **平时不跑任何 AI**（推荐状态）| **0GB**（一切按需加载，用完释放） |
| 仅转录 1 小时录音 | 约 3-4GB（Whisper large-v3 加载） |
| 仅 Ollama 摘要 | 约 10-11GB（qwen2.5:14b 加载） |
| 仅朗读（macOS say）| 约 50MB |
| 朗读（kokoro-onnx）| 约 500MB |

**关键点：**
- 转录和摘要**不会同时进行**（脚本是串行的）
- Ollama 默认 5 分钟无操作就卸载模型，自动释放内存
- 你 36GB 内存非常充裕，不会卡

---

## 五、磁盘占用最终统计

| 类别 | 大小 |
|------|------|
| Python 解释器（uv 管理） | 173MB |
| 转录环境（whisper-env） | 1.0GB |
| 朗读环境（tts-env） | 594MB |
| Ollama 模型（已有 + 即将下完） | 18GB |
| 脚本和文档 | <1MB |
| **总计** | **约 19.7GB** |

你 1TB 硬盘剩余 460GB+，毫无压力。

---

## 六、后续日常使用流程

### 配置阶段（仅做一次）

1. **音频路由**：按 `BlackHole配置指南.md` 配置聚合设备（5分钟）
2. **HF 令牌**（区分说话人需要）：运行 `~/meeting-ai/scripts/setup-hf-token.sh`

### 每次开会的流程

**录音（任意一种）：**
- QuickTime Player → 新建音频录制 → 选「会议录制输入」
- 或：`~/meeting-ai/scripts/record.sh`

**会后处理（一行命令）：**
```bash
cd ~/meeting-ai/scripts
./meeting.sh ~/meeting-ai/recordings/会议录音.m4a --hf-token $HF_TOKEN
```

**会得到 4 个文件：**
- 带时间戳的逐字稿（含说话人标签）
- SRT 字幕文件（可挂在视频上）
- 去口水词的清洗版逐字稿
- 结构化会议纪要（Markdown 格式）

### 通勤时听访谈/会议
```bash
./read.sh ~/meeting-ai/output/逐字稿.txt
```
自动用不同声音区分多个角色，生成 MP3 文件。

---

## 七、首次使用时还会下载什么

第一次跑转录时会自动从 HuggingFace 下载：
- **Whisper large-v3 模型**：约 1.5GB（仅一次，之后缓存）
- **Whisper 对齐模型**：约 500MB（仅一次）
- **pyannote 说话人区分模型**：约 200MB（需 HF 令牌，仅一次）

加完这些，全套占用 **约 22GB**。一次完整下载，永久使用。

---

## 八、需要注意的事

1. **没下完之前**，summarize.py 会自动用现有的 `qwen2.5-coder:14b` 顶替 — 它写代码强，写中文摘要也能用，但效果不如 qwen2.5:14b
2. **HF 令牌不是必须的**：不配置时转录依然正常，只是不区分说话人
3. **大文件转录慢一点正常**：1 小时录音首次约需 6-10 分钟（CPU 转录），之后会快一些
4. 升级 macOS 大版本后，BlackHole 可能需要重新装：`brew reinstall blackhole-2ch`
